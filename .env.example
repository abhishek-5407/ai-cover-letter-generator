# Server-side Gemini API Key (used by Vercel serverless function)
# This key is NEVER exposed to the browser
# Set this in Vercel Dashboard → Settings → Environment Variables
GEMINI_API_KEY=your_gemini_api_key_here
